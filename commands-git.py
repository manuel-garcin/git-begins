#pour cloner un repository
#dans un terminal, par exemple VS Code
git clone "adresse du repository"

#on ajoute le code souhaité

#on envoie le code sur GitHub
git add .
git commit -m "commentaire"
git push

#commande pour effectuer un pull
créer une nouvelle branche, apporter les modifications au fichier,
merge, accepter les modifs sur la branche principale
une fois dans le terminal de commande faire un
git push pour mettre à jour le fichier
avec les modifications apportés sur GitHub
